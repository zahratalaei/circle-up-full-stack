import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.pexels.com", "images.unsplash.com", "cdn.pixabay.com", "i.pinimg.com", "cdn.pixabay.com", "images.clerk.dev"],
  },
};

export default nextConfig;
